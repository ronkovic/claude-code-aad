# SPEC-008-T03: GhCliAdapter実装

## 複雑度: M

## 実装内容
- `infrastructure/src/adapters/gh_cli_adapter.rs` を実装
- GitHub CLI (`gh`) をラップするアダプタ
- PR作成、マージ、Issue操作を抽象化
- エラーハンドリングと出力パース

## 変更ファイル
- `crates/infrastructure/src/adapters/gh_cli_adapter.rs` (新規)
- `crates/infrastructure/src/adapters/mod.rs` (gh_cli_adapter追加)
- `crates/application/src/integration/integration_service.rs` (新規、依存)
- `crates/application/src/integration/mod.rs` (新規)
- `crates/application/src/lib.rs` (integration モジュール追加)

## テストケース
- PR作成のコマンド生成テスト
- PRマージのコマンド生成テスト
- Issue作成のコマンド生成テスト
- エラーハンドリングのテスト
- 出力パースのテスト

## 依存関係
- なし

## GitHub Issue
- TBD

## 受け入れ基準
- [ ] AC-3.1: `GhCliAdapter` 構造体が定義されている
- [ ] AC-3.2: `create_pr(title, body, base)` メソッドが実装されている
- [ ] AC-3.3: `merge_pr(number, strategy)` メソッドが実装されている
- [ ] AC-3.4: `create_issue(title, body, labels)` メソッドが実装されている
- [ ] AC-3.5: `gh` コマンドのエラーが適切にハンドリングされる
- [ ] AC-3.6: PR番号、Issue番号が出力から正しくパースされる
