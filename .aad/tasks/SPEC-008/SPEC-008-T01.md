# SPEC-008-T01: QualityService実装

## 複雑度: M

## 実装内容
- `application/src/quality/quality_service.rs` を実装
- フェーズごとのゲート条件検証ロジック
  - SPEC: 受入基準がテスト可能か
  - TASKS: 全タスクにIDが付与されているか
  - TDD: テスト成功、カバレッジ80%以上、Lint通過
- 検証結果レポート生成機能
- `QualityGate`, `QualityCheck` 構造体定義

## 変更ファイル
- `crates/application/src/quality/quality_service.rs` (新規)
- `crates/application/src/quality/mod.rs` (新規)
- `crates/application/src/lib.rs` (quality モジュール追加)
- `crates/domain/src/entities/quality_gate.rs` (新規)
- `crates/domain/src/entities/mod.rs` (quality_gate追加)

## テストケース
- フェーズゲートチェックのユニットテスト
- SPEC/TASKS/TDD各フェーズの検証テスト
- レポート生成のテスト
- 承認記録のテスト

## 依存関係
- なし

## GitHub Issue
- TBD

## 受け入れ基準
- [ ] AC-1.1: `application/src/quality/quality_service.rs` に `QualityService` が定義されている
- [ ] AC-1.2: `check_phase_gate(phase)` メソッドが実装されている
- [ ] AC-1.3: SPEC フェーズのゲート条件が検証される
- [ ] AC-1.4: TASKS フェーズのゲート条件が検証される
- [ ] AC-1.5: TDD フェーズのゲート条件が検証される
- [ ] AC-1.6: 検証結果がレポート形式で返される
