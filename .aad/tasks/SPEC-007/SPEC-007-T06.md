# SPEC-007-T06: 品質チェック

## 複雑度: S

## 実装内容
- 全クレートのビルド確認
- Clippy警告チェック（`-D warnings`モード）
- フォーマット確認（`cargo fmt --all --check`）
- 全テスト実行（`cargo test --all`）
- パフォーマンス検証（完了パターン検出 <10ms、タスク起動 <500ms）
- ドキュメント生成確認（`cargo doc --no-deps`）

## 変更ファイル
- なし（品質チェックのみ）

## テストケース
- 全テスト実行（application, cli, domain, infrastructure, tui）
- パフォーマンステスト
- 統合テスト

## 依存関係
- SPEC-007-T01（LoopEngine基盤）
- SPEC-007-T02（CompletionDetector）
- SPEC-007-T03（タスク進行ロジック）
- SPEC-007-T04（loop コマンド）
- SPEC-007-T05（TUI統合）

## 受け入れ基準
- [ ] `cargo build --all` が成功する
- [ ] `cargo clippy --all -- -D warnings` が警告なしで成功する
- [ ] `cargo fmt --all --check` がフォーマット問題なしで成功する
- [ ] `cargo test --all` が全て pass する
- [ ] 完了パターン検出時間が10ms以内
- [ ] タスク起動時間が500ms以内
- [ ] `cargo doc --no-deps` が成功する

## 実装時の注意点（過去の学びから）
- 各タスク完了時にClippyを実行していれば、この段階での警告は最小化される（SPEC-006の学び）
- パフォーマンステストは別途ベンチマークテストとして実装
- カバレッジ計測も実施（`cargo llvm-cov`）

## GitHub Issue
- 未作成
