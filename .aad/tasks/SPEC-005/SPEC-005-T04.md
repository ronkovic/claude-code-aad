# SPEC-005-T04: StyleFileAdapter実装

## 複雑度: M

## 実装内容

スタイル定義のファイル書き込み機能を実装します。

- `StyleFileAdapter` 構造体実装
- `write_style(style_name, tokens)` メソッド実装
- マーカー処理（`<!-- AAD_STYLE:BEGIN -->`, `<!-- AAD_STYLE:END -->`）
- 既存スタイルセクション置換ロジック

## 変更ファイル

### 新規作成
- `crates/infrastructure/src/persistence/style_file_adapter.rs`

### 変更
- `crates/infrastructure/src/persistence/mod.rs`

## テストケース

- [ ] `write_style()`で新規スタイルセクションが追加される
- [ ] 既存のスタイルセクションが正しく置換される
- [ ] マーカーが正しく挿入される
- [ ] マーカーが存在しない場合は末尾に追加される
- [ ] 複数のスタイルセクションが共存できる

## 依存関係

- SPEC-005-T01（persistence基盤）

## 受け入れ基準

- [ ] AC-4.1: `infrastructure/src/persistence/style_file_adapter.rs` が実装されている
- [ ] AC-4.2: `write_style(style_name, tokens)` メソッドが実装されている
- [ ] AC-4.3: マーカーが正しく使用される
- [ ] AC-4.4: 既存のスタイルセクションが正しく置換される
- [ ] AC-4.5: マーカーが存在しない場合は末尾に追加される

## 推定時間

4-6時間

## GitHub Issue

#44
