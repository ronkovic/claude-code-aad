# SPEC-005-T02: SpecJsonRepo実装

## 複雑度: M

## 実装内容

SpecエンティティのJSON永続化を実装します。

- `SpecJsonRepo` 構造体実装
- `SpecRepository` トレイト実装
- `find_by_id()`, `find_all()`, `save()`, `delete()` メソッド実装
- `.aad/data/specs/` ディレクトリ管理

## 変更ファイル

### 新規作成
- `crates/infrastructure/src/persistence/spec_json_repo.rs`

### 変更
- `crates/infrastructure/src/persistence/mod.rs`

## テストケース

- [ ] `SpecJsonRepo::new()`でインスタンスが作成できる
- [ ] `find_by_id()`でSpecが取得できる
- [ ] `find_all()`で全Specが取得できる
- [ ] `save()`でSpecがJSONファイルに保存される
- [ ] `delete()`でSpecが削除される
- [ ] 存在しないIDを指定した場合に`None`が返る

## 依存関係

- SPEC-005-T01（persistence基盤）

## 受け入れ基準

- [ ] AC-2.1: `SpecJsonRepo` が `SpecRepository` トレイトを実装している
- [ ] AC-2.2: `find_by_id()` でSpecが取得できる
- [ ] AC-2.3: `save()` でSpecがJSONファイルに保存される
- [ ] AC-2.4: `delete()` でSpecが削除される
- [ ] AC-2.5: JSONファイルが `.aad/data/specs/` に保存される

## 推定時間

4-6時間

## GitHub Issue

#42
