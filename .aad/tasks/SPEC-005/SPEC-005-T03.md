# SPEC-005-T03: TaskJsonRepo・SessionJsonRepo実装

## 複雑度: M

## 実装内容

TaskとSessionエンティティのJSON永続化を実装します。

- `TaskJsonRepo` 構造体実装
- `SessionJsonRepo` 構造体実装
- 各 Repository トレイト実装
- `.aad/data/tasks/`, `.aad/data/sessions/` ディレクトリ管理

## 変更ファイル

### 新規作成
- `crates/infrastructure/src/persistence/task_json_repo.rs`
- `crates/infrastructure/src/persistence/session_json_repo.rs`

### 変更
- `crates/infrastructure/src/persistence/mod.rs`

## テストケース

- [ ] `TaskJsonRepo`の基本操作（find_by_id, save, delete）が動作する
- [ ] `TaskJsonRepo::find_by_spec_id()`で特定Specのタスク一覧が取得できる
- [ ] `SessionJsonRepo`の基本操作（find_by_id, save, delete）が動作する
- [ ] `SessionJsonRepo::find_active()`でアクティブなセッション一覧が取得できる
- [ ] JSONファイルが適切なディレクトリに保存される

## 依存関係

- SPEC-005-T01（persistence基盤）
- SPEC-005-T02（SpecJsonRepo、参考実装として）

## 受け入れ基準

- [ ] AC-3.1: `TaskJsonRepo` が `TaskRepository` トレイトを実装している
- [ ] AC-3.2: `SessionJsonRepo` が `SessionRepository` トレイトを実装している
- [ ] AC-3.3: 各リポジトリの `find_by_id()`, `save()`, `delete()` が動作する
- [ ] AC-3.4: JSONファイルが適切なディレクトリに保存される

## 推定時間

4-6時間

## GitHub Issue

#43
