# SPEC-005-T01: persistence/モジュール基盤実装

## 複雑度: S

## 実装内容

persistence層の基盤となるモジュール構造とトレイト定義を実装します。

- `infrastructure/src/persistence/` ディレクトリ作成
- `FileStore` トレイト定義
- 共通エラー型 `PersistenceError` 定義
- `serde_json` クレート追加

## 変更ファイル

### 新規作成
- `crates/infrastructure/src/persistence/mod.rs`
- `crates/infrastructure/src/persistence/file_store.rs`

### 変更
- `crates/infrastructure/src/lib.rs` - persistenceモジュールを追加
- `crates/infrastructure/Cargo.toml` - serde_json, async-trait依存を追加

## テストケース

- [ ] `FileStore` トレイトが定義されている
- [ ] `PersistenceError` が定義されている
- [ ] モジュールがコンパイルできる

## 依存関係

- なし（最初のタスク）

## 受け入れ基準

- [ ] AC-1.1: `infrastructure/src/persistence/` ディレクトリが存在する
- [ ] AC-1.2: `serde_json` クレートが依存に含まれている
- [ ] AC-1.3: `FileStore` トレイトが定義されている
- [ ] AC-1.4: 共通のエラー型が定義されている

## 推定時間

2-4時間

## GitHub Issue

#41
