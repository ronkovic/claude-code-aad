# SPEC-005-T07: persistコマンド実装

## 複雑度: M

## 実装内容

セッション永続化のCLIコマンドを実装します。

- `cli/src/commands/persist.rs` 実装
- `aad persist save` サブコマンド実装
- `aad persist restore <timestamp>` サブコマンド実装
- `aad persist list` サブコマンド実装
- 確認メッセージUI実装

## 変更ファイル

### 新規作成
- `crates/cli/src/commands/persist.rs`

### 変更
- `crates/cli/src/commands/mod.rs`
- `crates/cli/src/main.rs`

## テストケース

- [ ] `aad persist save`で全セッション状態が保存される
- [ ] `aad persist restore <timestamp>`で指定時刻の状態に復元される
- [ ] `aad persist list`でバックアップ一覧が表示される
- [ ] 復元前に確認メッセージが表示される
- [ ] 存在しないタイムスタンプを指定した場合はエラーメッセージが表示される

## 依存関係

- SPEC-005-T02（SpecJsonRepo）
- SPEC-005-T03（TaskJsonRepo, SessionJsonRepo）
- SPEC-005-T06（BackupAdapter）

## 受け入れ基準

- [ ] AC-7.1: `cli/src/commands/persist.rs` が実装されている
- [ ] AC-7.2: `aad persist save` で全セッション状態が保存される
- [ ] AC-7.3: `aad persist restore <timestamp>` で指定時刻の状態に復元される
- [ ] AC-7.4: `aad persist list` でバックアップ一覧が表示される
- [ ] AC-7.5: 復元前に確認メッセージが表示される

## 推定時間

4-6時間

## GitHub Issue

#47
