# SPEC-009-T01: ensure-labels.shスクリプト作成

## 複雑度: S

## 優先度: Must

## Wave: 1 - 基盤・独立機能

## 対応要件: R6 - GitHub Issueラベル事前作成

## 実装内容
- 新規シェルスクリプト `.claude/scripts/ensure-labels.sh` を作成
- `gh label list` で既存ラベルを取得
- 必要なラベル（priority:*, size:*, status:*, type:*, spec:*）を定義
- 不足ラベルを `gh label create` で作成
- 色分け出力で結果を表示

## 変更ファイル
- `.claude/scripts/ensure-labels.sh` (新規作成)

## テストケース
- スクリプトが冪等性を持つ（再実行しても問題なし）
- SPEC-XXX引数で動的ラベル作成が動作する
- GitHub CLI未認証時にエラーメッセージを表示

## 依存関係
- なし（独立タスク）

## GitHub Issue
- （作成時に追記）
