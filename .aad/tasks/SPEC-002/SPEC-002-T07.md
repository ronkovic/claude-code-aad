# SPEC-002-T07: 品質チェック

## 基本情報

| 項目 | 内容 |
|------|------|
| タスクID | SPEC-002-T07 |
| SPEC | SPEC-002 |
| 複雑度 | S（1-4時間） |
| 優先度 | Must |
| 依存 | T01, T02, T03, T04, T05, T06 |
| 担当 | 未アサイン |

---

## 概要

SPEC-002 の全実装が完了した後、品質チェック（ビルド、テスト、Lint、フォーマット、カバレッジ）を実行し、受け入れ基準を満たすことを確認する。

---

## 作業内容

### 1. ビルド確認
```bash
cargo build --all
```

### 2. テスト実行
```bash
cargo test --all
```

### 3. Lintチェック
```bash
cargo clippy --all -- -D warnings
```

### 4. フォーマットチェック
```bash
cargo fmt --all -- --check
```

### 5. カバレッジ計測（オプション）
```bash
cargo llvm-cov --all --html
```

### 6. ドキュメント生成確認
```bash
cargo doc --no-deps
```

---

## 変更ファイル

なし（検証のみ）

---

## 受け入れ基準

### 非機能テスト
- [ ] `cargo build --all` が成功する
- [ ] `cargo test --all` が全て pass する
- [ ] `config/aad.toml` と `config/styles.toml` のサンプルファイルが作成されている
- [ ] ワークフロー状態遷移が正しく機能する
- [ ] 設定ファイルのバリデーションエラーが適切に表示される

### セキュリティ
- [ ] 設定ファイルのパスインジェクション対策が実装されている
- [ ] 不正な設定値による DoS 攻撃が防止されている

---

## テストコマンド

```bash
# 1. ビルド確認
cargo build --all

# 2. テスト実行
cargo test --all

# 3. Lintチェック
cargo clippy --all -- -D warnings

# 4. フォーマットチェック
cargo fmt --all -- --check

# 5. カバレッジ計測
cargo llvm-cov --all --html
open target/llvm-cov/html/index.html

# 6. ドキュメント生成
cargo doc --no-deps --open
```

---

## 期待結果

| チェック項目 | 期待結果 |
|-------------|----------|
| ビルド | エラーゼロ |
| テスト | 全テスト成功 |
| Clippy | 警告ゼロ（-D warningsモード） |
| Rustfmt | フォーマット問題なし |
| カバレッジ | 80%以上（推奨） |
| ドキュメント | 生成成功 |

---

## 備考

- このタスクは全タスク完了後に実行
- 問題があれば該当タスクに戻って修正
