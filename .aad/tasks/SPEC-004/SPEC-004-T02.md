# SPEC-004-T02: DependencyGraph実装

## 複雑度: M

## 実装内容

Spec間の依存関係を管理する`DependencyGraph`を実装します。

- `application/src/orchestration/dependency_graph.rs`に実装
- 依存関係の追加・削除メソッド
- トポロジカルソートアルゴリズムの実装
- 循環依存検出ロジック
- 依存関係の可視化（文字列表現）

## 変更ファイル

### 新規作成
- `crates/application/src/orchestration/dependency_graph.rs`

### 変更
- `crates/application/src/orchestration/mod.rs` - DependencyGraphを公開

## テストケース

- [ ] 依存関係の追加が正しく動作する
- [ ] トポロジカルソートが正しい実行順序を返す
- [ ] 循環依存が検出される
- [ ] 並列実行可能なSpecが識別される
- [ ] 依存関係グラフの文字列表現が正しい

## 依存関係

- SPEC-004-T01（Orchestrator構造体）

## 受け入れ基準

- [ ] `add_dependency(spec_id, depends_on)`メソッドが実装されている
- [ ] `topological_sort()`メソッドが実装されている
- [ ] `detect_cycle()`メソッドで循環依存を検出できる
- [ ] 並列実行可能なSpecのグループ化ができる

## 推定時間

5-7時間

## GitHub Issue

作成予定
