# SPEC-006-T01: TUIクレート作成

## 基本情報

| 項目 | 内容 |
|------|------|
| タスクID | SPEC-006-T01 |
| SPEC | SPEC-006 |
| 複雑度 | S（1-4時間） |
| 優先度 | Must |
| 依存 | なし |
| 担当 | 未アサイン |

---

## 概要

`crates/tui/` ディレクトリに TUI クレートを作成し、TUI 基盤を提供する。`ratatui` (v0.28) と `crossterm` を依存に含める。

---

## 作業内容

### 1. TUIクレートの作成
```bash
cd crates
cargo new tui --lib
cd tui
```

### 2. Cargo.tomlの設定
```toml
[package]
name = "tui"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.28"
crossterm = "0.28"
anyhow = "1.0"

[dependencies.domain]
path = "../domain"

[dev-dependencies]
```

### 3. lib.rsの基本構造作成
```rust
//! TUI (Terminal User Interface) クレート
//!
//! Ratatui によるリアルタイムダッシュボードを提供します。

pub mod app;
pub mod events;
pub mod views;
pub mod widgets;

pub use app::App;
```

### 4. 基本モジュールの作成
- `src/app.rs` - スタブ作成
- `src/events.rs` - スタブ作成
- `src/views/mod.rs` - スタブ作成
- `src/widgets/mod.rs` - スタブ作成

---

## 変更ファイル

- `crates/tui/Cargo.toml` - 新規作成
- `crates/tui/src/lib.rs` - 新規作成
- `crates/tui/src/app.rs` - 新規作成（スタブ）
- `crates/tui/src/events.rs` - 新規作成（スタブ）
- `crates/tui/src/views/mod.rs` - 新規作成（スタブ）
- `crates/tui/src/widgets/mod.rs` - 新規作成（スタブ）

---

## 受け入れ基準

### AC-1.1: `crates/tui/Cargo.toml` が存在する
```bash
test -f crates/tui/Cargo.toml
```

### AC-1.2: `ratatui` クレート（v0.28）が依存に含まれている
```bash
grep "ratatui.*0.28" crates/tui/Cargo.toml
```

### AC-1.3: `crossterm` クレートが依存に含まれている
```bash
grep "crossterm" crates/tui/Cargo.toml
```

### AC-1.4: `cargo build -p tui` が成功する
```bash
cargo build -p tui
```

---

## テストコマンド

```bash
# ビルド確認
cargo build -p tui

# テスト実行（初期スタブのみ）
cargo test -p tui

# Lintチェック
cargo clippy -p tui -- -D warnings
```

---

## 期待結果

| チェック項目 | 期待結果 |
|-------------|----------|
| クレート作成 | crates/tui/ が存在 |
| 依存関係 | ratatui 0.28, crossterm が含まれる |
| ビルド | エラーゼロ |

---

## 備考

- このタスクは基盤となるクレートの作成のみ
- 実装の詳細はT02以降で行う
- スタブファイルは最小限のコードで問題なし
