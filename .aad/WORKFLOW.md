# 6フェーズワークフロー詳細

自律型AI駆動開発の標準ワークフローです。

## 📋 ワークフロー概要

```
SPEC → TASKS → TDD → REVIEW → RETRO → MERGE
```

各フェーズには明確な完了条件（品質ゲート）があり、次のフェーズに進む前に必ず確認します。

---

## Phase 1: SPEC（仕様書作成）

### 目的
機能要件を明確化し、実装可能な形式で文書化する

### 作業内容

1. **SPEC-TEMPLATE.md をコピー**
   ```bash
   cp .aad/templates/SPEC-TEMPLATE.md .aad/specs/SPEC-001.md
   ```

2. **仕様書を記入**
   - 概要・目的
   - MoSCoW要件（Must/Should/Could/Won't）
   - UI/UX要件
   - 技術要件
   - データモデル
   - API仕様
   - 受け入れ基準（テスト可能な形式）

3. **レビュー**
   - 技術レビュー
   - ビジネスレビュー

4. **承認取得**
   - SPEC-001.md の「✅ 承認」セクションを記入

### 品質ゲート

- [ ] 受け入れ基準がテスト可能な形式で記述されている
- [ ] MoSCoWで優先度が設定されている
- [ ] API仕様が明確（該当する場合）
- [ ] データモデルが定義（該当する場合）
- [ ] **⚠️ 人間承認必須**

### チェック方法

```bash
/aad:gate SPEC
```

### 次のフェーズへ

品質ゲート通過後、TASKSフェーズへ：

```bash
/aad:tasks SPEC-001
```

---

## Phase 2: TASKS（タスク分割）

### 目的
SPECを実装可能な小さなタスクに分割し、並列実行計画を立てる

### 作業内容

1. **タスク分割実行**
   ```bash
   /aad:tasks SPEC-001
   ```

2. **自動実行される内容**
   - SPEC-001.md を読み込み
   - Must/Should/Could要件をタスクに分割
   - タスクID付与（SPEC-001-T01, T02...）
   - 複雑度推定（S/M/L）
   - 依存関係の明記
   - タスクファイル作成（tasks/SPEC-001/）
   - GitHub Issues作成
   - HANDOFF.md更新

3. **タスク確認**
   ```bash
   /aad:status SPEC-001
   ```

### タスクサイズの目安

| サイズ | 所要時間 | 説明 |
|--------|----------|------|
| S | 1-4時間 | 単一機能、単純な変更 |
| M | 4-8時間 | 複数ファイル、中程度の複雑度 |
| L | 8時間以上 | 大規模変更、可能なら分割推奨 |

### 依存関係の例

```
T01: データベーススキーマ [並列可能]
T02: 認証API ← T01
T03: フロントエンドUI ← T02
T04: パスワードリセット ← T02
T05: ソーシャルログイン ← T02
```

### 品質ゲート

- [ ] 全タスクにID（SPEC-XXX-TXX）が付与されている
- [ ] 依存関係が明記されている
- [ ] 複雑度（S/M/L）が設定されている
- [ ] GitHub Issuesが作成されている
- [ ] HANDOFF.mdが更新されている
- [ ] **⚠️ 人間承認必須**

### チェック方法

```bash
/aad:gate TASKS
```

### 次のフェーズへ

品質ゲート通過後、TDDフェーズへ：

**手動モード**:
```bash
/aad:worktree SPEC-001-T01
```

**自動モード**:
```bash
/aad:orchestrate SPEC-001
```

---

## Phase 3: TDD（テスト駆動開発）

### 目的
テスト先行でタスクを実装し、品質を担保する

### 作業内容

1. **worktree環境で作業**
   ```bash
   cd ../my-project-T01
   claude --dangerously-skip-permissions  # Docker環境
   ```

2. **TDDサイクル**
   ```
   Red → Green → Refactor
   ```

   #### Red（失敗するテストを書く）
   ```typescript
   test('should create user', () => {
     const user = createUser({email: 'test@example.com'});
     expect(user.id).toBeDefined();
   });
   ```

   #### Green（最小限の実装で通す）
   ```typescript
   function createUser(data: {email: string}) {
     return {id: generateId(), email: data.email};
   }
   ```

   #### Refactor（リファクタリング）
   ```typescript
   function createUser(data: UserInput): User {
     validateEmail(data.email);
     return {
       id: generateUuid(),
       email: normalizeEmail(data.email),
       createdAt: new Date(),
     };
   }
   ```

   #### コミットのベストプラクティス

   **推奨**: Red→Green→Refactorサイクルごとに小さくコミット

   ```bash
   # Greenフェーズ完了後
   git add .
   git commit -m "test(SPEC-001-T01): add user creation test"

   # Refactorフェーズ完了後
   git add .
   git commit -m "refactor(SPEC-001-T01): improve email validation"
   ```

   **または**: 品質ゲート合格時にまとめてauto-commit

   ```bash
   /aad:gate TDD --auto-commit
   ```

3. **継続的チェック**
   ```bash
   npm test               # テスト実行
   npm run test:coverage  # カバレッジ確認
   npm run lint           # Lint実行
   ```

4. **PR作成**
   ```bash
   gh pr create --draft --title "SPEC-001-T01: データベーススキーマ"
   ```

5. **品質ゲートとauto-commit（オプション）**
   ```bash
   # 品質チェックのみ
   /aad:gate TDD

   # 品質チェック + 自動コミット
   /aad:gate TDD --auto-commit
   ```

   `--auto-commit` を使用すると、品質ゲート合格時に自動的にコミットが作成されます：
   - Conventional Commits形式
   - カバレッジ、テスト結果を含む
   - タスクIDとIssue番号を自動リンク

### 品質ゲート

- [ ] 全テストgreen
- [ ] カバレッジ80%以上
- [ ] Lint通過（0 errors）
- [ ] Type check通過
- [ ] `gh pr create --draft`でPR作成完了

### チェック方法

```bash
/aad:gate TDD
```

### 次のフェーズへ

品質ゲート通過後、REVIEWフェーズへ：

```bash
gh pr ready  # DraftからOpenに変更
```

---

## Phase 4: REVIEW（コードレビュー）

### 目的
コード品質を確保し、チーム知見を共有する

### 作業内容

1. **AI自己レビュー**
   - コードスタイルの一貫性
   - ベストプラクティスの遵守
   - セキュリティ脆弱性チェック
   - パフォーマンス懸念の確認

2. **CI実行**
   - GitHub Actionsで自動テスト
   - Lintチェック
   - カバレッジ測定
   - セキュリティスキャン

3. **人間レビュー**
   - ロジックの妥当性
   - 設計の適切性
   - 可読性・保守性
   - テストの十分性

### レビュー観点

#### コード品質
- [ ] コーディングルールに準拠
- [ ] 複雑度が適切（循環的複雑度 < 10）
- [ ] 適切な命名（意図が明確）
- [ ] コメントが適切（複雑な部分のみ）

#### 設計
- [ ] SOLID原則に準拠
- [ ] 適切な抽象化レベル
- [ ] 依存関係が適切
- [ ] 拡張性が考慮されている

#### テスト
- [ ] エッジケースがカバーされている
- [ ] テストが意図を表現している
- [ ] テストが独立している
- [ ] テストデータが適切

#### セキュリティ
- [ ] 入力検証が適切
- [ ] SQLインジェクション対策
- [ ] XSS対策
- [ ] 認証・認可が適切

### 品質ゲート

- [ ] AI自己レビュー完了
- [ ] CI green
- [ ] コンフリクトなし
- [ ] セキュリティスキャン通過
- [ ] **⚠️ 人間承認必須**

### チェック方法

```bash
/aad:gate REVIEW
```

### 次のフェーズへ

品質ゲート通過後、RETROフェーズへ：

```bash
/aad:integrate SPEC-001-T01
```

---

## Phase 5: RETRO（振り返り）

### 目的
学びを蓄積し、プロセスを継続的に改善する

### 作業内容

1. **振り返り実行**
   ```bash
   /aad:retro SPEC-001
   ```

2. **記録内容**
   - ✅ うまくいったこと（Keep）
   - 🔧 改善できること（Problem）
   - 💡 次回試すこと（Try）
   - 📚 技術的な学び
   - 📊 品質メトリクス

3. **CLAUDE.md更新**
   - 学びの蓄積セクションに追加
   - コーディングルール更新（必要に応じて）
   - プロセス改善の反映

### 振り返りの観点

#### プロセス
- タスク分割は適切だったか？
- 見積もりは正確だったか？
- 並列実行は効果的だったか？
- コミュニケーションは円滑だったか？

#### 技術
- 選択した技術は適切だったか？
- パフォーマンスは期待通りか？
- テスト戦略は効果的だったか？
- ドキュメントは十分か？

#### 品質
- バグの発生率は？
- カバレッジは十分だったか？
- レビュー指摘は多かったか？
- リファクタリングの必要性は？

### 品質ゲート

- [ ] docs/aad/retrospectives/にログが作成されている
- [ ] Keep/Problem/Tryが記載されている
- [ ] 技術的な学びが明記されている
- [ ] CLAUDE.md更新提案がある

### チェック方法

```bash
/aad:gate RETRO
```

### 次のフェーズへ

品質ゲート通過後、MERGEフェーズへ：

```bash
# 自動的にMERGEフェーズに移行
# （/aad:integrateで既にマージ済みの場合）
```

---

## Phase 6: MERGE（統合・クリーンアップ）

### 目的
変更をデフォルトブランチに統合し、環境をクリーンアップする

### 作業内容

1. **最終確認**
   ```bash
   /aad:status SPEC-001
   ```

2. **worktree削除**
   - `/aad:integrate`で自動削除済み
   - 手動削除の場合: `git worktree remove ../my-project-T01`

3. **Issue閉鎖確認**
   - GitHub Issuesが全て閉鎖されているか確認

4. **デプロイ（該当する場合）**
   - staging環境へのデプロイ
   - smoke test実行
   - 本番環境へのデプロイ

### 品質ゲート

- [ ] mainマージ完了
- [ ] Issue閉鎖
- [ ] worktree削除
- [ ] デプロイ成功（該当する場合）

### チェック方法

```bash
/aad:gate MERGE
```

### 完了

SPEC-001が完了しました！

次のSPECへ：

```bash
/aad:retro SPEC-001  # 最終振り返り
# 次のSPECを開始
```

---

## ワークフローのバリエーション

### 手動モード（人間が調整役）

```bash
# 1. SPEC作成・承認
/aad:gate SPEC

# 2. タスク分割
/aad:tasks SPEC-001
/aad:gate TASKS

# 3. 各タスクを順次実行
/aad:worktree SPEC-001-T01
# (作業...)
/aad:integrate SPEC-001-T01

# 4. 振り返り
/aad:retro SPEC-001
```

### 自動モード（AIが調整役）

```bash
# 1. SPEC作成・承認
/aad:gate SPEC

# 2. 全自動実行
/aad:orchestrate SPEC-001

# （自動で完了）
```

### ハイブリッドモード

```bash
# 1-2. SPEC・タスク分割（人間）
/aad:gate SPEC
/aad:tasks SPEC-001
/aad:gate TASKS

# 3. 並列実行（AI）
/aad:orchestrate SPEC-001 --from=TDD

# 4. 振り返り（人間）
/aad:retro SPEC-001
```

---

## コンテキスト管理

### 70%ルール

作業中は常にコンテキスト使用率を監視：

```bash
/aad:context
```

| 使用率 | アクション |
|--------|------------|
| 70%到達 | `/aad:handoff` 実行 |
| 新セッション | `HANDOFF.md` を読み込んで再開 |

### セッション分割の推奨タイミング

- 大きなタスク（L）の開始前
- レビュー待ち期間
- 自然な区切り（フェーズ完了時）

---

## トラブルシューティング

### テストが通らない

```bash
# ローカルで確認
npm test -- --verbose

# カバレッジ詳細
npm run test:coverage -- --verbose

# 特定のテストのみ実行
npm test -- path/to/test.spec.ts
```

### CI失敗

```bash
# ログ確認
gh run view

# 再実行
gh run rerun
```

### コンフリクト発生

```bash
# デフォルトブランチを取り込む（CLAUDE.mdで設定されたブランチ）
git fetch origin
git merge origin/<default-branch>

# コンフリクト解決後
git add .
git commit
git push
```

**注**: `<default-branch>` は CLAUDE.md で設定されたブランチ（通常は main または master）です。

---

## 参考リンク

- [COMMANDS.md](COMMANDS.md) - コマンドリファレンス
- [QUALITY-GATES.md](QUALITY-GATES.md) - 品質ゲート詳細
- [SETUP.md](SETUP.md) - 初期セットアップ
- [CUSTOMIZE-CHECKLIST.md](CUSTOMIZE-CHECKLIST.md) - カスタマイズ手順
