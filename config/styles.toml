# スタイルシステム設定ファイル
# 既存の switch-style.sh の動作を Rust に移植する際に使用

# ========================================
# Standard スタイル（デフォルト）
# ========================================
[styles.standard]
# メッセージプレフィックス
completion = "完了："
result = "結果："
notice = "通知："
error = "エラー："

# コンテキストバー警告レベル
[styles.standard.context_warnings]
caution = "🟡 通知：注意"
warning = "🟠 通知：警告"
critical = "🔴 通知：危機的"
limit = "⛔ 通知：限界"

# ========================================
# Sage スタイル（簡潔・高密度）
# ========================================
[styles.sage]
# メッセージプレフィックス
completion = "成功しました："
result = "解："
notice = "告："
error = "否："

# コンテキストバー警告レベル
[styles.sage.context_warnings]
caution = "🟡 告：中程度"
warning = "🟠 告：警告レベル"
critical = "🔴 告：危機的"
limit = "⛔ 告：限界"

# ========================================
# バックアップ設定
# ========================================
[backup]
# 保持するバックアップの最大数
max_count = 3

# バックアップディレクトリ（プロジェクトルートからの相対パス）
directory = ".aad/backups/styles"

# ========================================
# 変換対象ファイル設定
# ========================================
[conversion]
# 変換対象のパターン（globパターン）
target_patterns = [
    ".claude/commands/aad/*.md",
    ".claude/scripts/context-bar.sh",
    "CLAUDE.md"
]

# 除外パターン（globパターン）
exclude_patterns = [
    ".aad/backups/**",
    ".git/**"
]
